import Foundation

/// Holds the mutable runtime state of a level in progress (dot position, timer, tap count, etc.).
/// Placeholder — fields populated in STORY-005.
final class LevelRuntimeState: ObservableObject {
    // TODO: add runtime state fields
}
