import SwiftUI

/// Root view — placeholder for the app coordinator to wire up screens.
struct ContentView: View {
    var body: some View {
        Text("Tiny Routes")
            .font(.largeTitle)
            .foregroundColor(.primary)
    }
}

#Preview {
    ContentView()
}
